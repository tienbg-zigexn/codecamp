<% flash.each do |type, msg| %>
  <div class="<%= type %> notice">
    <p class="notice__message"><%= msg %></p>
    <button class="notice__close" type="button" data-controller="removals" data-action="click->removals#remove">Close</button>
  </div>
<% end %>
