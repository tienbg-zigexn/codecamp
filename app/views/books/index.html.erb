<% content_for :title, "Book List" %>

<%= render "search_form" %>

<%= link_to "New Book", new_book_path, class: "button" %>

<% @books.each do |book| %>
  <%= render "book", book: book %>
<% end %>
