<%= form_with url: books_path, method: :get, data: { controller: "search", turbo_frame: "books" } do |f| %>
  <%= f.text_field :query, placeholder: "Search books...", data: { action: "input->search#search" } %>
<% end %>
