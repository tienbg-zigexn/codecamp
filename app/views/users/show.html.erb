<%= turbo_stream_from @user %>
<% content_for :title, @user.email_address %>
<% unless Current.user == @user %>
  <article id="<%= nested_dom_id @user, Current.user, 'follow_status' %>">
    <%= render 'follow_status' %>
  </article>
<% end %>
<%= render 'followers' %>
<%= render 'following' %>
<%= render 'reviews' %>
